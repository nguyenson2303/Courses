/* alert() : Dùng để tạo ra Popup.
 *confirm(): Dùng để tạo ra Popup kèm theo xác nhận .
 *prompt(): Dùng để tạo ra Popup xác nhận kèm theo ô input và xác nhận.
 *setTimeout(): Dùng để chạy 1 đoạn code sau một khoảng thời gian, 1000 = 1 giây.
 *setInterval(): Dùng để chạy liên tục 1 đoạn code sau mỗi khoảng thời gian, 1000 = 1 giây.
 */ 
setTimeout (function () {
    prompt('Đây là alert');
},2000);
